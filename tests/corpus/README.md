# Regression corpus

These are minimized cases derived from real mission and mod reports. Each
entry in `manifest.json` records the source context and diagnostic codes that
must remain detectable. Keep cases small and add a new file when a production
bug is fixed so parser and analysis changes cannot silently reintroduce it.
